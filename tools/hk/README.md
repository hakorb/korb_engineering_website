# HK Tools (Password Protected)

Add tools by dropping `.html` files here and registering in `app.js`:

```js
hk: {
  tools: [
    { name: 'Tool Name', file: './tools/hk/tool-name.html' }
  ]
}
```

No tools registered yet.
